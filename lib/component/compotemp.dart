import 'package:flutter/cupertino.dart';

class CompoTemp extends StatelessWidget {
  const CompoTemp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
